# Mission Control — Dev environment
# Copy this to deploy/.env.dev on the server and fill in secrets.

NODE_ENV=production
NEXT_PUBLIC_APP_ENV=dev

# Placeholder — MICO-9 will define auth requirements
# NEXTAUTH_SECRET=change-me
# NEXTAUTH_URL=https://mission-control-dev.morpheuxx.meimberg.io
