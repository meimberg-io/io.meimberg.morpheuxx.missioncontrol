# Mission Control — Production environment
# Copy this to deploy/.env.prod on the server and fill in secrets.

NODE_ENV=production
NEXT_PUBLIC_APP_ENV=prod

# Placeholder — MICO-9 will define auth requirements
# NEXTAUTH_SECRET=change-me
# NEXTAUTH_URL=https://mission-control.morpheuxx.meimberg.io
